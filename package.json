{
  "name": "DSP-GainStructure-Emulator",
  "private": true,
  "description": "AEC commissioning tool (RMLR / ELR) — zero npm dependencies",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/test-qrc.js && node test/test-discovery.js && node test/test-roles.js && node test/test-advisor.js && node test/test-server.js"
  }
}
